/*package operacioneslistas;

public class OperacionesListasEjem {

    void main(String[] args) {
        /*Nodo lista = new Nodo(18);
        Nodo nuevo = new Nodo(39);
        lista.sig = nuevo;
        nuevo = new Nodo(10);
        lista.sig.sig = nuevo;
        nuevo = new Nodo(56);
        lista.sig.sig.sig = nuevo;
        nuevo = new Nodo(71);
        lista.sig.sig.sig.sig = nuevo;
        nuevo = new Nodo(100);
        lista.sig.sig.sig.sig.sig = nuevo;

        Nodo temp = lista;
        */
        /*Nodo lista = new Nodo(5);
        Nodo aux = lista, nuevo;

        for (int i = 10; i <= 100; i += 5) {
            nuevo = new Nodo(i);
            aux.sig = nuevo;
            aux = nuevo;
        }

        Nodo temp = lista;
        while (temp != null) {
            System.out.print(temp.info + "  ");
            temp = temp.sig;
        }
        System.out.println();
    }
}*/
